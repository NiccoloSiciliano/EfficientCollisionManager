#include "../../LinkedList.hpp"
/*

template <typename T> int LinkedList<T>::get_count()
{
    return this->count;
}


template <typename T> T* LinkedList<T>::iterate()
{
    if (index < count)
    {
        T *hold = this->current->data;
        this->current = this->current->next;
        index++;
        return hold;
    }
    return NULL;
}
*/